<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/darker_gray"
    tools:context=".BaslaMainActivity">

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alwaysDrawnWithCache="false"
            android:animateLayoutChanges="false"
            android:animationCache="false"
            android:background="#459C27B0"
            android:baselineAligned="false"
            android:divider="@drawable/ic_launcher_background"
            android:fadeScrollbars="true"
            android:focusable="auto"
            android:focusableInTouchMode="true"
            android:isScrollContainer="false"
            android:keepScreenOn="false"
            android:orientation="vertical"
            android:visibility="visible"
            tools:visibility="visible">

        <ImageView
                android:id="@+id/imageView2"
                android:layout_width="match_parent"
                android:layout_height="123dp"
                app:srcCompat="@mipmap/logofelan" />

        <Button
                android:id="@+id/egitmen"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="#97FF00"
                android:bufferType="spannable"
                android:cursorVisible="false"
                android:gravity="center_horizontal|center_vertical"
                android:text="@string/egitmen"
                android:textColor="#F44336"
                android:textSize="18sp"
                android:textStyle="bold"
                android:translationY="60dp" />

        <Button
                android:id="@+id/sporcu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="#9C27B0"
                android:gravity="center_horizontal|center_vertical"
                android:text="@string/sporcu"
                android:textColor="#F44336"
                android:textSize="18sp"
                android:textStyle="bold"
                android:translationY="60dp" />

    </LinearLayout>
</LinearLayout>